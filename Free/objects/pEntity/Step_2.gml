/// @description Entity Loop
// You can write your code in this editor

if (!global.gamePaused){
}

flash = max(flash - 0.04, 0);
if(entityActivateScript != -1 and entityActivateScript != -1){
	if(point_distance(x,y,oPlayer.x,oPlayer.y) <= entityInteractDistance){
		DisplaySprite(entityInteractSprite, 0, id, entityInteractDistance);
	}
}
